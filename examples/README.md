This directory contains examples of how to use the go-huggingface library.

They add tests, but they can be expensive to run, since they download large models/datasets from the internet,
and models can be large. So their tests are no run in CI by default.
